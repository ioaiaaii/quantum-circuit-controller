# fake-osaka — IBM Eagle r3, 127 qubits, ECR basis.  Third member of
# the same-architecture, different-calibration triple; see
# fake-brisbane.yaml header for the variance-isolation argument.
apiVersion: qcc.io/v1alpha1
kind: QPU
metadata:
  name: fake-osaka
  labels:
    app.kubernetes.io/name: quantum-circuit-controller
    app.kubernetes.io/managed-by: kustomize
    qcc.io/provider: local
    qcc.io/family: eagle-r3
spec:
  provider: local
  kind: simulator
